version: "2"
linters:
  default: standard
  enable:
    - revive
formatters:
  enable:
    - gci
    - gofmt
  settings:
    gci:
      sections:
        - standard
        - default
        - localmodule
