# This is an example .goreleaser.yml file with some sensible defaults.
# Make sure to check the documentation at https://goreleaser.com

# The lines below are called `modelines`. See `:help modeline`
# Feel free to remove those if you don't want/need to use them.
# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

before:
  hooks:
    - go mod tidy
    - mise run generate

builds:
  - main: ./cmd/pve-imds 
    goos:
      - linux
    goarch:
      - amd64

archives:
  - formats: binary
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}-
      {{- tolower .Os }}-
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

nfpms:
  - maintainer: Wyatt Anderson <wanderson@gmail.com>
    formats:
      - deb
    bindir: /usr/sbin
    contents:
      - src: release/linux/usr/lib/systemd/system/pve-imds.service
        dst: /usr/lib/systemd/system/pve-imds.service
        file_info:
          mode: 0644
    scripts:
      postinstall: release/scripts/deb/postinstall.sh
      preremove: release/scripts/deb/preremove.sh
    deb:
      compression: zstd

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).